[tox]
envlist = py39,py313
skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
    requests
    pybip39
    dotenv
commands =
    coverage run -m pytest
    coverage xml

[coverage:run]
relative_files = True
source = .
branch = True
